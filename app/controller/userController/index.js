const { signupUser, } = require(`./signupUser`);
const { loginUser, } = require(`./loginUser`);
const { checkout, } = require(`./checkout`);

module.exports = {
    signupUser,
    loginUser,
    checkout,
};
