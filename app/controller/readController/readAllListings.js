/** Reads all listings in database;
 * you may send next, skip and category id in query
 * @param {Request} req Express request object
 * @param {Response} res Express response object
 */
async function readAllListings(req, res) {

}

module.exports = {
    readAllListings,
};
